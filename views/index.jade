extends layout

block content
  h1= title
  p Welcome to the party  #{title}
  ul
    for item in logs
      li #{item}

  form(action='/add' method='POST')
    input(type='text' name='email')
    button(type='submit') Add
